version: "2"

sql:
  - engine: postgresql
    schema:
      - "db/migrations"
    queries:
      - "db/queries"
    gen:
      go:
        package: "queries"
        out: "internal/queries"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_pointers_for_null_types: true
